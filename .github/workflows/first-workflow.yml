name: First KodeKloude Workflow
on: push
jobs:
    workflow-tester:
        runs-on: ubuntu-22.04
        steps:
            - name: Checkout
              uses: actions/checkout@v6

            - name: Say Hello
              run: echo "Hello Sourabh from KodeKloude !!"
            
            - name: LIsting files
              run: ls -a
              
